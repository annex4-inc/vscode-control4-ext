{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "JSON schema for Control4 default and experience icons",
  "definitions": {
    "experienceicons": {
      "additionalProperties": false,
      "type": "object",
      "properties": {
        "id": {
          "description": "The ID of the icon",
          "type": "string"
        },
        "type": {
          "id": "propertyType",
          "type": "string",
          "enum": [
            "Default_Icon",
            "State_Icon"
          ]
        },
        "iconstate": {
          "description": "The state of the icon",
          "type": "string"
        },
        "sizes": {
          "type": "array",
          "description": "An array of image sizes",
          "items": {
            "type": "string"
          }
        },
        "relpath": {
          "description": "The relative path from the root dir without leading or trailing /",
          "examples": ["icons/driver"],
          "type": "string"
        }
      },
      "required": ["id", "type", "sizes", "relpath"]
    }
  },
  "type": "array",
  "minItems": 0,
  "uniqueItems": true,
  "items": {
    "allOf": [
      {
        "$ref": "#/definitions/experienceicons"
      }
    ]
  }
}